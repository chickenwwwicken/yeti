---
id: 2.python-setup
aliases: []
tags: []
next: "[[3.gemini]]"
prev: "[[1.build-ai-agent]]"
---
Let's set up a virtual environment for our project.

Virtual environments are Python's way to keep dependencies (e.g. the Google AI libraries we're going to use) separate from other projects on our machine.

## Assignment

1. [ ] Use `uv` to create a new project. It will create the directory and also initialize git.

```bash
uv init your-project-name
cd your-project-name
```

2. [ ] Create a virtual environment at the top level of your project directory:

```bash
uv venv
```

	Always add the `venv` directory to your `.gitignore` file.

3. [ ] Activate the virtual environment:

```bash
source .venv/bin/activate
```

You should see `(your-project-name)` at the beginning of your terminal prompt, for example, mine is:

```
(aiagent) wagslane@MacBook-Pro-2 aiagent %
```

	Always make sure that your virtual environment is activated when running the code or using the Boot.dev CLI.

4. [ ] Use `uv` to add two dependencies to the project. They will be added to the file `pyproject.toml`:

```
uv add google-genai==1.12.1
uv add python-dotenv==1.1.0
```

This tells Python that this project requires [`google-genai`](https://pypi.org/project/google-genai/) version `1.12.1` and the [`python-dotenv`](https://pypi.org/project/python-dotenv/) version `1.1.0`.

To run the project using the `uv` virtual environment, you use:

```bash
uv run main.py
```

In your terminal, you should see `Hello from YOUR PROJECT NAME`

**Run and submit** the CLI tests while in your virtual environment.

Run the CLI commands to test your solution.

1. uv pip list
    - Expecting exit code: 0
    - Expecting stdout to contain all of:
        
        - google-genai
        - python-dotenv
        

